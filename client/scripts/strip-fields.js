const schemaPath = 'createListing';
const { jsonSchema, filterFields } = require(`jsonSchemas/${schemaPath}`);

// strip unwanted fields
// modified certain fields
// add ui schema for certain fields
// add ordering
